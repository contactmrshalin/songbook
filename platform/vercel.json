{
  "framework": "nextjs",
  "root": "platform",
  "installCommand": "yarn install --no-lockfile --ignore-scripts",
  "buildCommand": "node scripts/prebuild.mjs && next build",
  "git": {
    "deploymentEnabled": {
      "gh-pages": false
    }
  }
}
