{
  "buildCommand": "yarn build",
  "installCommand": "yarn install --no-lockfile --ignore-scripts",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "gh-pages": false
    }
  }
}
