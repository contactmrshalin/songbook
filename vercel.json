{
  "buildCommand": "cd platform && node scripts/prebuild.mjs && next build",
  "installCommand": "yarn install --no-lockfile --ignore-scripts",
  "outputDirectory": "platform/.next",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "gh-pages": false
    }
  }
}
